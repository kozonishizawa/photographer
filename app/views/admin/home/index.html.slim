.l-dashboard
  .l-dashboard__inner
    h1.l-dashboard__heading メインメニュー
    .l-dashboard__menu
      - admin_menu.each do |menu|
        = link_to menu[:url], class: 'l-dashboard__card' do
          h2.l-dashboard__name = menu[:name]
          p.l-dashboard__lead = menu[:description]